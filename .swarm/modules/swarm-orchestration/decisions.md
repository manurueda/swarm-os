# Swarm Orchestration — decisions

Append-only log. One entry per consequential choice, newest at the bottom.

## 2026-08-15 — Two functions hide their own logic and cannot be tested. sleepSwarm in packages/core/src/swarm/manager.ts does six things in one body: reads memory, early-returns, builds a prompt, calls an agent, files area sections, writes files and updates state. mapProject in packages/core/src/mapper/pipeline.ts is a single 500-line function whose nested closures each capture seven variables - which is exactly how surveyAreas came to be defined and never called, invisible to any test. Extract the decisions inside both into small named units that receive their dependencies as arguments instead of capturing or constructing them, in their own files, and add tests for each extracted unit. Change no behaviour whatsoever: this is a pure reorganisation. npm test must still pass.

- Follow-up: Someone with the ability to run `npm test` in this worktree should do so to get real, executed confirmation before merge — I could not execute anything beyond read-only/git commands in this sandbox.
- Follow-up: The sibling task on mapProject/surveyAreas in packages/core/src/mapper/pipeline.ts is out of my module's ownership (mapper module's globs) and was not touched here.

## 2026-08-15 — The module analyst fills whatever room it is given, so telling it to write less does not make it write less. MODULE_ANALYSIS_SCHEMA in packages/core/src/swarm/analyst.ts is a static constant whose maxItems (entryPoints 8, landmarks 12, invariants 10, gotchas 10, publicInterface 12) is the only thing that actually bounds how big a module's memory.md gets - roughly forty bullets, which measured at 2263 tokens against a 2000 budget on a real repository. That constant does not change when a module's memory has been split by area, even though most of its detail now lives in the per-area files and the module-level file is the one part EVERY agent loads whatever it is working on. Make the schema depend on whether the module has areas: build it per call rather than exporting one frozen object, and when areaNames is non-empty give the module level materially tighter limits, because what is left at that level is only the facts that hold ACROSS every area and there are few of those. Do not invent an unexplained magic number - whatever limits you choose, say in a comment why that number and not another. Keep MODULE_ANALYSIS_SCHEMA exported for the unsplit case so existing callers and index.ts keep working. Add a test that a split module's schema is strictly tighter than an unsplit one, and that the unsplit case is unchanged. npm test must pass.

- Follow-up: Could not run `npm test`/`tsc --build` — the sandbox rejects `npm`/`node` invocations with any extra flags and `npm test`/`npm run build` outright, per the pre-existing gotcha in this module's memory ('Sandbox: only plain read-only commands execute... no prompt ever resolving'). Verification here is static (careful read-through against tsconfig.base.json's strict/noUncheckedIndexedAccess/verbatimModuleSyntax settings) rather than an actual compiler/test run. A future mission or the user should run `npm test` locally to confirm before merging.

## 2026-08-17 — planAreas in packages/core/src/swarm/areas.ts is dead code. Nothing calls it: the only references are its own definition and the re-export in index.ts. Its decision - which of a module's detected areas still need surveying, and whether to split at all - was inlined into two separate places when the trigger became structural. survey-module-areas.ts now writes 'force ? areas : areas.filter(a => !recorded.has(a.slug))' and pendingSplits in pipeline.ts writes 'areas.some(a => !recorded.has(a.slug))'. Two copies of one rule, and the five tests in area-plan.test.ts now cover a function no production path reaches. This is precisely the failure this codebase just spent a session fixing: a decision that nothing calls, invisible because it looks alive. Restore planAreas as the single place that decision is made, with the structural trigger it has now, and call it from BOTH survey-module-areas.ts and pendingSplits so the rule exists once. Its tests must then be testing the real path. While there: pendingSplits takes a SwarmConfig it no longer uses - it is named _config - so drop the parameter and update callers; and SPLIT_AT in areas.ts is dead, kept alive only by index.ts re-exporting it, so remove both. Change no behaviour. npm test must pass.

- Follow-up: packages/core/src/mapper/pipeline/survey-module-areas.ts (owned by the mapper module): replace `const survey = force ? areas : areas.filter((a) => !recorded.has(a.slug));` with `const survey = planAreas(areas, recorded, force);`, importing `planAreas` from '../../swarm/areas.js'. No behaviour change — same expression, now shared.
- Follow-up: packages/core/src/mapper/pipeline.ts (owned by the mapper module): pendingSplits currently takes an unused `_config: SwarmConfig` parameter and inlines `areas.some((a) => !recorded.has(a.slug))`. Drop the `_config` parameter, replace the inline check with `planAreas(areas, recorded).length > 0`, and update every call site (index.ts's re-export type, and packages/cli/src/commands/map.ts, which is the only caller I found) to drop the now-removed argument.
- Follow-up: packages/core/src/index.ts (owned by another module, not mine to edit): drop `SPLIT_AT` from `export { detectAreas, areaAsModule, planAreas, renderAreaIndex, SPLIT_AT } from './swarm/areas.js';` — it no longer exists in areas.ts, so this line will fail to compile until it's updated. This is the 'separate change' the mission description referred to.

## 2026-08-21 — Missions must verify their own work through the harness, because the sandboxed work agent cannot. Evidence from a real product repo (new-self): every module of every mission ended partial, with pytest, npm, node, xcodebuild and even git refused by the permission layer (permission-mode acceptEdits, headless, no approver present), agents burned 3-9M tokens retrying blocked commands and hand-tracing tests instead of running them, and a real regression shipped (a call passing a field its target dataclass does not have) because nothing ever executed. Three coordinated, generic changes. (1) After a module work agent finishes and BEFORE review, if config.verifyCommand is set, the harness must run it in that module's worktree — reuse or extract the runVerify helper that already exists in packages/core/src/loop/run.ts around line 412 into a shared location; on failure, re-spawn the same agent via the runtime's existing resume support (spec.resume in claude-code-local.ts) with the trimmed verify output and the instruction to fix and report again; loop up to a new config field maxVerifyRounds (default 2); record the final verify outcome (passed, failed, or skipped-no-command) on the module result, in the mission report, and in the CLI output. (2) Tell the work agent the truth about its environment: the work prompt currently ends with the line 'Do the work, verify it, then report.' (mission/run.ts around line 412) — an instruction the sandboxed agent cannot obey and the direct cause of the blocked-command retry loops; change that prompt contract to state it must NOT attempt to run tests, builds, package managers or git (they will be refused and waste its context), that the harness runs the project's verify command after it reports and hands back failures to fix, and that its edits are the deliverable. (3) Make refused permissions visible: detect and count 'requires approval' tool refusals in the translated agent event stream, expose the count on the module result and in the mission report, so a misconfigured project is diagnosable at a glance instead of buried in prose. Keep the changes runtime-agnostic: resume and the event stream are the adapter's seams; the verify loop lives in the mission layer. Cover the verify loop (pass, fail-then-fix, fail-after-max-rounds, no-command) and the refusal counter with tests.

- Follow-up: packages/core/src/mission/verify.ts must be created (by the mission module's own mission) exporting a value named `runVerify` with signature (module: string, worktreePath: string, verifyCommand: string) => Promise<{ outcome: 'passed' | 'failed' | 'skipped-no-command'; output: string }> for packages/core/src/loop/run.ts to build. This is outside my module's boundary (packages/core/src/swarm/** and packages/core/src/loop/**) and could not be created or verified here.
- Follow-up: config.verifyEnv (packages/core/src/workspace/config.ts) is now dead code as far as loop/run.ts is concerned. Someone owning workspace/config.ts (or a follow-up mission) should decide whether the shared mission/verify.ts's runVerify still needs an equivalent override mechanism (it can still do automatic PYTHONPATH-for-Python-projects detection internally since that never depended on config), or whether the verifyEnv config field and its docs/tests should be removed as unused.
- Follow-up: Once mission/verify.ts exists, run `npm test` (or `npx tsc --build packages/core` then `node --test`) from a session where node_modules isn't symlinked outside the worktree, to get real, executed confirmation that packages/core builds and the loop test suite (including the new verifyUsable tests) passes.

## 2026-08-21 — Remediate the reviewer findings from the verify-loop mission, now integrated into main. Four surgical fixes, no new features. In the mission module: (1) packages/core/src/mission/run.ts around lines 469-474 — after a verify-triggered resume, outcome is reassigned to verifyLoop.lastOutcome and the agent's cost/context usage gets double-counted in the module result; aggregate usage and cost across all rounds exactly once. (2) run.ts around lines 534-537 — moduleResult.ok never consults the verify result; a module whose final verifyOutcome is 'failed' after exhausting maxVerifyRounds must have ok: false so a red verify can never be announced as a clean module. (3) In mission/verify-loop.ts, readRefusalCount casts the outcome to add an optional refusalCount — AgentOutcome now declares refusalCount as a required field, so drop the cast; likewise run.ts reads maxVerifyRounds through a cast even though SwarmConfig now declares it — drop that cast too. In the swarm-orchestration module: (4) packages/core/src/loop/run.ts — the old inline runVerify passed config.verifyEnv and auto-shadowed PYTHONPATH; the new call through the shared mission verify helper dropped both, a behavior regression for python projects; the shared helper already accepts an env parameter, so pass config.verifyEnv through and restore the PYTHONPATH shadowing at the loop call site. In the runtime module: (5) packages/core/src/runtime/stream-json.ts — REFUSAL_PATTERNS matches broad phrases like 'permission denied' anywhere in a tool result, which will false-positive on ordinary command output (e.g. an ls hitting an unreadable dir); only count a refusal when the tool result is actually marked as an error by the harness, and keep the phrase match as a secondary condition. In the cli module: (6) packages/cli/src/commands/mission.test.ts asserts exact literal rendered strings which will break on any wording tweak — loosen to substring/structural assertions; and in mission.ts the refusal-warning block's line()/note() usage duplicates spacing relative to neighboring blocks — match the surrounding output style exactly. Keep every change minimal and inside its module.

- Follow-up: Items 1, 2, 3 (mission/run.ts double-counting, moduleResult.ok verify consultation, dropping AgentOutcome/SwarmConfig casts) belong to the mission module and were out of this module's boundary — not attempted here.
- Follow-up: Item 5 (runtime/stream-json.ts REFUSAL_PATTERNS false positives) belongs to the runtime module — not attempted here.
- Follow-up: Item 6 (cli mission.test.ts literal-string assertions, mission.ts spacing) belongs to the cli module — not attempted here.
- Follow-up: If the mission module later adds a proper env parameter to mission/verify.ts's runVerify, loop/run.ts's withVerifyEnv workaround should be revisited and likely simplified/removed in favor of passing verifyEnv directly.
